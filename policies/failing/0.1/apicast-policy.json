{
  "$schema": "http://apicast.io/policy-v1/schema#manifest#",
  "name": "Example policy",
  "summary": "An example policy that fails successfullyto be used in integration tests.",
  "version": "1.0.0",
  "configuration": {
    "type": "object",
    "properties": {
      "fail_access": {
        "type": "boolean",
        "description": "if true the policy fails on access phase"
      }
    },
    "required": ["fail_access"]
  }
}
